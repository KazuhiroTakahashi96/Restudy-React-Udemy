import Item from "./Item";

const PackingList = () => {
  return (
    <div className="list">
      {/* <ul>{<Item item={item} key={item.id} />}</ul> */}
    </div>
  );
};

export default PackingList;
